
import SwiftUI

@main
struct abc2_updatedApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

struct TooltipConfiguration {
    var targetElement: String
    var tooltipText: String
    var textSize: Int
    var paddingValue: Int
    var textColor: Color
    var backgroundColor: Color
    var cornerRadius: Int
    var tooltipWidth: Int
    var arrowWidth: Int
    var arrowHeight: Int 
}


